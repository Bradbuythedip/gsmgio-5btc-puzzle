# Pre-registration: item E residue (2026-09-26), committed before the run
Candidates with no clean trace in harness/logs, from the user's item E: the empty password,
`b45a5e3d827593ca` (P32T salt as text), the VIC sentence in uppercase, `HALFANDBETTERHALF`.
(VIC lowercase, halfandbetterhalf, and the last-words block are traced to campaigns 01/06/10/16/32.)
Frozen protocol: 16 decrypts each + freeze oracle + scalar checks. No variants. If null, item E is closed.
