# Pre-registration: the five checkpoint preimages, verbatim (2026-09-26)

Committed before the run. Each is a creator-confirmed stage answer (compressed-P2PKH(sha256(X)) received
a `3GSMG24T…` OP_RETURN stamp, tick 59). The attempt logs show clean runs only for some of them, and the
149-digit VIC string has no literal harness/log trace. So all five run once with the gate's frozen
functions (16 decrypts each, 80 total) plus the P32T freeze oracle. The 227-char string is excluded;
it is spent (campaign 01/05, tick 57).

1. `causality`
2. `1GSMG1JC9wtdSwfwApgj2xcmJPAwx7prBe`
3. the 149-digit VIC string (phase3.2.txt digits)
4. `jacquefrescogiveitjustonesecondheisenbergsuncertaintyprinciple`
5. `theflowerblossomsthroughwhatseemstobeaconcretesurface`

Stop rule: no variants. If null, the checkpoint family is closed as password material.
